# .env.example
#
# Purpose:
#   Defines the environment variables needed to run the Product Inventory System API.
#   Copy this file to `.env` and replace placeholder values with your own.
#
# Variables:
#   - MONGODB_URI: Connection string for your MongoDB database.
#       Example (local): mongodb://127.0.0.1:27017/product_inventory
#       Example (Atlas): mongodb+srv://<username>:<password>@cluster0.mongodb.net/product_inventory
#
#   - PORT: Port number for the Express server to listen on.
#       Example: 3000
#
# Notes:
#   - Do not commit your real `.env` file with credentials to GitHub.
#   - This `.env.example` is safe to commit, since it only shows placeholders.

MONGODB_URI=mongodb://127.0.0.1:27017/product_inventory
PORT=3000